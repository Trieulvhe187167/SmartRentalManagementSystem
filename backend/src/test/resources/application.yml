spring:
  datasource:
    url: jdbc:h2:mem:rental_management;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
app:
  security:
    jwt:
      secret: test-secret-test-secret-test-secret-test-secret
      access-token-expiration-minutes: 120
      issuer: rental-management-api-test
